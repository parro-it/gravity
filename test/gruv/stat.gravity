#unittest {
    name: "test stat";
    error: NONE;
    result: 4096;
};


func main() {
    var results;
    Gruv.mkdir('/tmp/aTest', func() {
        Gruv.stat('/tmp/aTest', func(err, result) {
            results = result['size'];
        });
    });

    Gruv.startLoop();
    return results;
}
