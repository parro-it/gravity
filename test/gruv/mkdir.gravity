#unittest {
    name: "test mkdir";
    error: NONE;
    result: null;
};


func main() {
    var results;
    Gruv.rmdir('/tmp/aTest', func() {
        Gruv.mkdir('/tmp/aTest', func(err, result2) {
            results = err;
        });
    });

    Gruv.startLoop();
    return results;
}
