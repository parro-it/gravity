#include 'src/optional/assertion/assertion.gravity'

#unittest {};

test('Create a directory', func () {
    Gruv.rmdir('/tmp/aTest', func() {
        Gruv.mkdir('/tmp/aTest', func(err, result2) {
            Assert.equals(null, err);
        });
    });

    Gruv.startLoop();
});

func main() {}
