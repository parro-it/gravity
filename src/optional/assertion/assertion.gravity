class Assert {
    static func equals(expected, actual) {
        if (expected == actual) {
            return;
        }

        Fiber.abort('expected ' + expected + ', but actual value is ' + actual)
    }
}

func test(name, fn) {
    fn();
}

